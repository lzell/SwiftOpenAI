//
//  AIProxy.swift
//  SwiftOpenAIExample
//
//  Created by Lou Zell on 3/25/24.
//


OptionsListView(openAIService: OpenAIServiceFactory.service(apiKey: apiKey, organizationID: localOrganizationID))

struct AIProxy {

    /* ------------------------- Begin placeholder --------------------------------- */
    ///
    /// Instructions for integration:
    /// 1. Drop this file into your Xcode project
    /// 2. Replace this section with the constants that you received at dashboard.aiproxy.pro
    /// 3. Read the integration examples directly following this placeholder
    ///
    //private let aiproxyPartialKey = "<partial-key-goes-here>"
    //#if DEBUG && targetEnvironment(simulator)
    //private let aiproxyDeviceCheckBypass = "<device-check-bypass-goes-here>"
    //#endif
    /* -------------------------- End placeholder ---------------------------------- */


    private let aiproxyPartialKey = ""
    #if DEBUG && targetEnvironment(simulator)
    private let aiproxyDeviceCheckBypass = ""
    #endif

    static var isConfigured: Bool {
        return aiProxy

    }}


    init

    import Foundation
